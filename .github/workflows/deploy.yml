name: Deploy (Production)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
      NOTION_DATABASE_PARTNERS: ${{ secrets.NOTION_DATABASE_PARTNERS }}
      NOTION_DATABASE_PROJECTS: ${{ secrets.NOTION_DATABASE_PROJECTS }}
      NOTION_DATABASE_TEAM: ${{ secrets.NOTION_DATABASE_TEAM }}
      NOTION_DATABASE_TEXTBLOCKS: ${{ secrets.NOTION_DATABASE_TEXTBLOCKS }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Restore Next.js cache
        uses: actions/cache@v5
        with:
          path: .next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('**pnpm-lock.yaml') }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build with Next.js
        run: pnpm build

      - name: Upload artifact for custom deploy
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-out
          path: ./out
          retention-days: 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: nextjs-out
          path: ./out

      - name: Deploy via Rsync Action (with Password)
        uses: up9cloud/action-rsync@v1.4
        env:
          HOST: ${{ secrets.SSH_HOST }}
          USER: ${{ secrets.SSH_USER }}
          PASSWORD: ${{ secrets.SSH_PASSWORD }}
          TARGET: ${{ secrets.SSH_DIR }}/
          SOURCE: "out/"
          ARGS: "-avz --delete"
